# Secrets
.env
*.env
!.env.example
infra/cloudflared/*.json
infra/cloudflared/cert.pem

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / Next.js
node_modules/
.next/
out/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Build / data
dist/
build/
*.sqlite
data/
pgdata/

# OS / editor
.DS_Store
.idea/
*.swp

# The cluster's certificate authority and the node map. secrets/docker-ca/ca.key
# signs every GPU server's Docker API certificate — committing it would hand
# anyone with the repo root on all of them.
secrets/

# Local AI-assistant tooling: skills, hooks and settings installed per machine.
# These belong to the workstation, not the project, and are large enough to
# swamp the repository.
.agent/
.agents/
.claude/
.gemini/
.codex/
